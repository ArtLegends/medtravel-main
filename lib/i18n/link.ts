// // lib/i18n/link.ts
// import { isLocale, type Locale } from "./config";

// export function withLang(path: string, lang: Locale) {
//   const clean = path.startsWith("/") ? path : `/${path}`;
//   return isLocale(clean.split("/")[1]) ? clean : `/${lang}${clean}`;
// }
