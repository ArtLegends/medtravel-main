"use client";

import {
  Card as HeroCard,
  CardBody as HeroCardBody,
  CardHeader as HeroCardHeader,
  Button as HeroButton,
  Link as HeroLink,
  Input as HeroInput,
  Switch as HeroSwitch,
  Chip as HeroChip,
} from "@heroui/react";

// Переэкспортируем компоненты для использования в Server Components
export const Card = HeroCard;
export const CardBody = HeroCardBody;
export const CardHeader = HeroCardHeader;
export const Button = HeroButton;
export const Link = HeroLink;
export const Input = HeroInput;
export const Switch = HeroSwitch;
export const Chip = HeroChip;
